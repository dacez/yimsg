# 线上网页真实操作测试 TODO

> 主要对照：[`线上网页真实操作测试方案.md`](线上网页真实操作测试方案.md) 与 `https://yimsg.im/app/` 的实际页面。
> 最后复核：2026-07-26。
> 触发更新：每个真实操作用例完成、阻塞、复测或发现 Bug 时立即更新。
> 入口关系：上级索引见 [`README.md`](../README.md)；本文只记录 2026-07-26 这一轮执行状态和证据摘要。

## 0. 执行信息

- [x] `ENV-01` 拉取最新 `origin/main`：`2f1961d`。
- [x] `ENV-02` 使用隔离的无痕浏览器会话打开 `https://yimsg.im/app/`。
- [x] `ENV-03` 登录页可见功能初步盘点。
- [x] `ENV-04` 注册 A、B、C、D、E，记录 UID 和页面对应关系。
- [ ] `ENV-05` **FAIL → BUG-014**：多个页面在不刷新时可保持不同内存会话，但任一页面重新登录后，其他页面刷新会切换成最后登录账号。
- [x] `ENV-06` 准备图片和普通文件，验证页面选择器可读取。
- [ ] `ENV-07` **FAIL → BUG-017**：`./tools/run_all_tests.sh` 已完整运行；protocol codegen、构建、文档、Go 单测、Server / CLI / Agent E2E、SDK unit / integration、UIKit、Web unit 均通过，Playwright UI 为 134 passed、1 failed、2 flaky。

状态说明：勾选表示 `PASS`；`FAIL`、`BLOCKED`、`N/A` 必须在条目后直接标注并引用 Bug 或阻塞记录。

## 1. 启动、注册、登录和设置

- [x] `AUTH-01` 首次启动选择 Instant + Auto。
- [x] `AUTH-02` 首次启动选择 Persistent + Desktop。
- [x] `AUTH-03` 手动选择 Mobile 后认证页仍可完整操作。
- [x] `AUTH-04` 注册 A 成功并进入聊天页。
- [x] `AUTH-05` 注册 B、C、D、E 成功。
- [x] `AUTH-06` 重复用户名注册显示 `username already exists`。
- [x] `AUTH-07` 注册必填字段浏览器校验。
- [x] `AUTH-08` 错误密码登录显示 `wrong password` 且不进入应用。
- [x] `AUTH-09` 不存在账号登录显示 `user not found`。
- [x] `AUTH-10` 正确密码登录、退出到认证页、再次登录成功；同 token 多页面退出一致性受 BUG-013 影响。
- [x] `AUTH-11` 修改密码缺少字段显示 `Fill in both fields`，旧密码错误显示 `Failed: wrong old password`。
- [x] `AUTH-12` E 修改密码成功后强制回到登录页，旧密码失效、新密码登录成功。
- [x] `SET-01` 设置页显示昵称、UID、存储模式。
- [ ] `SET-02` **FAIL → BUG-007**：E 修改昵称成功且重新登录后保留，但 B 的好友和会话仍显示旧昵称。
- [ ] `SET-03` **FAIL → BUG-007**：E 上传头像成功，但 B 的好友、会话和群成员仍显示旧的字母头像。
- [x] `SET-04` 中英文切换覆盖导航、设置和聊天文案，切回 English 正常。
- [x] `SET-05` Instant 隐藏清除数据卡片。
- [x] `SET-06` Persistent 清除数据后重新拉取到服务端已有好友。

## 2. 通讯录和好友

- [x] `CT-01` 好友 / 请求 / 搜索页签切换。
- [x] `CT-02` 搜索不存在账号显示 `User not found`。
- [x] `CT-03` A 搜索 B 并发送带备注好友申请。
- [ ] `CT-04` **FAIL → BUG-001**：A（Persistent）的已发申请列表为空；C（Instant）同路径可正常显示。
- [ ] `CT-05` **FAIL → BUG-001**：B 接受后 B 立即看到 A，A 刷新页签仍看不到 B；A 清除本地数据全量重拉后才出现 B。
- [x] `CT-06` C 向 D 申请，D 拒绝，双方待处理状态消失。
- [x] `CT-07` B 修改 E 备注为 `E-备注-R1`，通讯录立即显示备注。
- [x] `CT-08` B 单向删除 D 后 B 列表移除 D；D 端按产品设计仍保留 B，历史会话未被删除。
- [x] `CT-09` B 再次向 D 申请并由 D 接受，双方列表恢复，B 保留新备注 `D-重新添加`。
- [ ] `CT-10` **BLOCKED**：已有好友关键字经过防抖后正确过滤；群聊已由全局搜索覆盖，测试组织在执行组织删除用例后已不存在，无法完成组织关键字复核。
- [x] `CT-11` 申请和好友列表反复切换、重新拉取后未出现重复条目。
- [ ] `CT-12` **BLOCKED**：本轮严格使用 5 个真实注册账号，未制造超过 40 个好友的前置数据。
- [x] `CT-13` 群备注、收藏、取消收藏和重新收藏均成功。
- [x] `CT-14` B 屏蔽 E 后 E 发信被拒绝；取消屏蔽后发信恢复。
- [x] `CT-15` B 对 E 开启和关闭免打扰，按钮图标状态正确切换。

## 3. 单聊

- [x] `DM-01` 从 B 的联系人详情打开与 A 的单聊。
- [x] `DM-02` 按发送按钮发送文本，双方可见。
- [x] `DM-03` Enter 发送；Shift+Enter 换行不发送。
- [x] `DM-04` 空白消息不发送。
- [x] `DM-05` Emoji 选择、插入和发送。
- [x] `DM-06` Markdown 模式发送并安全渲染格式。
- [x] `DM-07` 图片上传、发送、对端预览。
- [x] `DM-08` 文件上传、发送、文件名 / 大小 / 下载。
- [x] `DM-09` 发送中占位快速出现并被服务端结果替换。
- [ ] `DM-10` **FAIL → BUG-010**：引用消息发送和引用块内容正确，但点击引用块不跳转到原消息。
- [ ] `DM-11` **FAIL → BUG-009**：单条成功转发到 A 单聊和群聊，但接收端“click to view”卡片无法打开内容。
- [ ] `DM-12` **FAIL → BUG-009**：两条消息合并转发成功，但转发详情同样无法打开。
- [x] `DM-13` 发送方撤回，双方同步为撤回状态。
- [x] `DM-14` 超过撤回时限后入口或错误正确。
- [x] `DM-15` 单聊详情显示昵称、`@username`、免打扰和屏蔽状态；B 侧会话标题正确使用备注。
- [x] `DM-16` 免打扰开启 / 关闭，操作图标正确切换。
- [x] `DM-17` 屏蔽后对端发信受限，取消屏蔽后恢复。
- [x] `DM-18` 220 条未读显示 `99+`，打开会话后徽标清除。
- [x] `DM-19` E 的 B 单聊（19:28）排在群聊（19:17）之前。
- [x] `DM-20` 220 条单聊消息跨页双向拉取，连续窗口顺序稳定且无重复。
- [x] `DM-21` 220 条覆盖 150 条窗口裁剪、向前 / 向后翻页、最早 / 最新边界和滚动锚点。

## 4. 群聊和群管理

- [x] `GRP-01` 空群名不能建群。
- [x] `GRP-02` 未选成员不能建群。
- [x] `GRP-03` B 选择 A、E 建群成功，三端可见。
- [x] `GRP-04` 群主和普通成员都能发送消息。
- [x] `GRP-05` 群内图片、文件、Emoji、Markdown 均在成员端正确显示。
- [x] `GRP-06` 输入 `@` 打开成员选择器并搜索 A。
- [x] `GRP-07` `@A` 消息三端内容正确。
- [x] `GRP-08` `@全体成员` 消息三端内容正确。
- [x] `GRP-09` 单聊输入 `@` 不出现群成员选择器。
- [ ] `GRP-10` **FAIL → BUG-009 / BUG-010**：群内引用发送成功，单条和多选转发到群成功；引用不跳转、转发详情打不开。
- [x] `GRP-11` E 撤回群消息，A、B、E 三端同步为撤回状态。
- [x] `GRP-12` 详情显示成员数、群主和成员。
- [ ] `GRP-13` **FAIL → BUG-011**：群主 B 上传成功，但已在线的 A、E 不更新群头像。
- [x] `GRP-14` 群主添加 D，D 能收到后续消息；D 只能看到入群后的系统消息和后续消息。
- [x] `GRP-15` 群主移出 D，D 不再收到新消息；D 再发送时服务端拒绝。
- [ ] `GRP-16` **FAIL → BUG-002**：普通成员 D 可通过真实页面把好友 C 加入群聊。
- [x] `GRP-17` 群备注、收藏 / 取消收藏、重新收藏、免打扰开关均成功。
- [x] `GRP-18` 120 条群消息完整拉取，`LOAD-G-001` 至 `LOAD-G-120` 连续、有序、唯一；接收端分页到最早消息。
- [ ] `GRP-19` **BLOCKED**：本轮仅注册 5 个真实账号，无法形成超过 40 名成员的真实群。

## 5. 组织管理

- [x] `ORG-01` B 创建组织并在通讯录看到入口。
- [x] `ORG-02` 进入空组织并返回。
- [ ] `ORG-03` **FAIL → BUG-005**：重命名服务端成功，C 显示新名称，但发起操作的 B 一直显示旧名称。
- [x] `ORG-04` 创建一级和二级部门，面包屑正确。
- [x] `ORG-05` 二级部门重命名与删除成功；当前管理面板名称更新仍受 BUG-005 影响。
- [x] `ORG-06` 添加 C、E，设置职务和排序。
- [x] `ORG-07` 移除 C，C 的通讯录入口立即消失。
- [x] `ORG-08` 授予 C 管理员，重新打开管理面板后管理员列表更新。
- [x] `ORG-09` C 作为管理员成功添加 E。
- [ ] `ORG-10` **FAIL → BUG-006**：撤销 C 后服务端拒绝其操作，但 C 的管理入口和已打开面板仍保留。
- [ ] `ORG-11` **FAIL → BUG-006**：普通组织成员也显示完整管理入口和全部写操作按钮。
- [x] `ORG-12` 删除测试组织后 B、C、E 的通讯录入口消失；已打开详情仍受 BUG-005 影响。

## 6. 搜索、同步、多页面和压力

- [x] `SEARCH-01` 全局搜索 `E-备注` 命中联系人备注。
- [x] `SEARCH-02` 全局搜索命中 `PROFILE-E-R1-message-008` 并打开对应单聊；详情联动受 BUG-008 影响。
- [x] `SEARCH-03` 无结果显示 `No matching messages`，Cancel 恢复会话列表。
- [x] `SEARCH-04` 当前会话搜索命中、跳转后出现 `msg-highlight`，并自动关闭搜索栏。
- [x] `SYNC-01` B 同账号两个页面同时在线，页面 2 发群消息后页面 1 收到。
- [x] `SYNC-02` B 页面 1 发群消息后页面 2 收到。
- [x] `SYNC-03` 页面 1 保持群聊已读，页面 2 离开聊天页期间未累计该群未读。
- [ ] `SYNC-04` **FAIL → BUG-016**：页面 1 修改好友备注后，页面 2 等待约 3.6 秒仍显示旧值；手动切换 Requests / Friends 后才更新。
- [ ] `SYNC-05` **FAIL → BUG-012**：页面 2 停留 Settings 时会话预览更新，但返回 Chat 后当前群消息列表不追平。
- [ ] `SYNC-06` **FAIL → BUG-014**：B 的 Instant 页面刷新后直接加载为最后在另一页面登录的 D，UID 和会话均串号，且仍显示 B 页面原有的 Instant 模式。
- [ ] `SYNC-07` **BLOCKED**：当前无痕浏览器控制接口没有页面级离线开关，不能在不影响其他并发账号的前提下严格制造短时断网。
- [x] `LOAD-01` 120 条群聊、220 条单聊均通过真实输入框分批高速发送，批次结束无 `Sending…` 残留。
- [x] `LOAD-02` 多会话按最后消息重排；单聊 150 条窗口双向分页时首尾锚点连续。
- [x] `LOAD-03` 约 2200 字符的中英文、换行和特殊字符消息双端完整显示。
- [x] `SEC-01` `<img onerror>` 和 `<script>` 按文本显示，DOM 中转义为 `&lt;...&gt;`，未创建可执行节点。
- [x] `MOBILE-01` D 使用 Mobile 布局完成会话列表、聊天、详情和 Back 返回。
- [ ] `MOBILE-02` **BLOCKED**：当前浏览器控制接口不提供可验证持续时长的触屏长按；消息操作按钮路径已覆盖，但不能冒充长按通过。
- [x] `MOBILE-03` Desktop 布局保持三栏，消息、详情和通讯录操作按钮均可用。

## 7. Bug 记录

### BUG-001 [P1] Persistent 模式好友申请与接受结果不增量同步

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；A 为 Persistent/Desktop，B、C 为 Instant。
- 账号：A `qa260726a7421`，B `qa260726b7421`。
- 前置条件：A、B 均为新注册且无好友。
- 步骤：
  1. A 搜索 B，填写备注 `B-测试好友` 并发送申请。
  2. A 打开“Requests”；B 打开“Requests”并接受。
  3. A 等待同步状态结束，切换 Requests / Friends 强制重新拉取。
  4. A 在 Settings 执行 Clear Data，再回到 Friends。
- 期望：A 发送后看到不可操作的已发申请；B 接受后 A、B 无需清本地库即可互相出现在好友列表。
- 实际：A 的已发申请始终为空；B 接受后 B 立即看到 A，A 的好友列表仍为空。A 执行 Clear Data 后，B 才以备注 `B-测试好友` 出现在 A 好友列表。
- 复现率：1/1；对照组 C（Instant）发送给 D 后，C 的已发申请可正常显示。
- 影响：Persistent 用户无法可靠看到好友申请和接受结果，好友主链路在普通刷新操作下表现失败；清除全部本地数据是破坏性较强的临时绕过。
- 初步定位：Persistent DataGateway 的联系人增量同步或好友通知后的主动拉取链路。

### BUG-002 [P1] 普通群成员可把自己的好友加入群聊

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 为群主，D 为普通成员。
- 账号：B `qa260726b7421`，C `qa260726c7421`，D `qa260726d7421`。
- 前置条件：B 创建 `QA群聊-20260726` 并邀请 A、E，随后群主正常添加 D；C 与 D 是好友，但 C 尚未入群。
- 步骤：
  1. D 以普通成员身份进入群详情。
  2. 点击页面显示的 `Add Member`。
  3. 选择 D 的好友 C 并确认。
  4. 在 B、C、D 页面观察群成员和系统消息。
- 期望：普通成员不显示添加成员入口，或服务端拒绝该操作；只有群主或明确授权的管理员能加人。
- 实际：D 的详情页显示 `Add Member`，确认后提示 `Member added`；成员数从 4 变为 5，C 收到 `QA成员C joined the group` 并获得群会话。
- 复现率：1/1。
- 影响：任意普通成员都可绕过群主管理边界扩散群成员，属于真实可利用的越权。
- 初步定位：`packages/uikit/src/app/views/chat/detail-panel.ts` 无条件渲染并绑定添加成员按钮；`server/internal/service/group.go` 的 `AddGroupMember` 未校验请求者是否为群主或管理员。

### BUG-003 [P3] 英文界面的多类服务端文案仍显示中文

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；A、B 的界面语言均为 English。
- 账号：A `qa260726a7421`，B `qa260726b7421`。
- 前置条件：A、B 已互为好友并进入单聊。
- 步骤：
  1. A 发送 `recall-now-005`，在撤回时限内执行 Recall。
  2. 被移出群的 D 尝试继续发群消息。
  3. B 屏蔽 E 后，E 尝试向 B 发送私聊。
- 期望：系统消息和业务错误均使用当前 English 界面的英文文案。
- 实际：撤回占位分别显示 `你撤回了一条消息`、`对方撤回了一条消息`；被移出群发信显示 `Failed to send message: 非群员`；被屏蔽发信显示 `Failed to send message: 对方暂不接受私聊`，均与 English 界面混排。
- 复现率：三类路径均为 1/1。
- 影响：国际化不完整，不影响数据正确性。
- 初步定位：`server/internal/service/message_recall.go` 直接生成中文系统文本；部分 service 错误消息也直接使用中文，前端按当前语言拼接错误时无法本地化。

### BUG-004 [P2] 被移出群后页面仍保留可发送的输入框和陈旧成员详情

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；D 为 Persistent/Mobile。
- 账号：B `qa260726b7421`（群主），D `qa260726d7421`（被移除成员）。
- 前置条件：D 已加入 `QA群聊-20260726`，正停留在该群详情页。
- 步骤：
  1. B 在群详情移除 D，B 页面显示 `Member removed`，群成员数从 5 变为 4。
  2. D 等待同步；D 的群详情仍显示 5 名成员且保留 `Add Member`。
  3. D 返回群聊，页面已收到 `QA访客D was removed from the group`，但附件、Emoji、Markdown、输入框和 Send 均保持可操作。
  4. D 输入 `GROUP-D-after-removed-should-fail-007` 并发送。
- 期望：收到移除事件后立即刷新成员与当前用户身份状态；历史可保留，但发送区应禁用并明确提示已被移出。
- 实际：详情页保留移除前的 5 人陈旧数据；聊天页允许编辑并提交，之后才显示 `Failed to send message: 非群员`。失败消息未送达其他成员。
- 复现率：1/1。
- 影响：用户会误以为仍可发言，完成输入后才丢失内容；Mobile 详情状态与服务端实际权限不一致。
- 初步定位：群成员变更通知只触发消息同步，当前详情和 composer 未据此刷新成员身份；错误消息同时存在未本地化中文。

### BUG-005 [P2] 组织重命名、移除和删除后当前详情不刷新

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 为组织创建者，C、E 为成员。
- 账号：B `qa260726b7421`，C `qa260726c7421`，E `qa260726e7421`。
- 前置条件：B 已创建 `QA组织-20260726`。
- 步骤：
  1. B 将组织重命名为 `QA组织-20260726-R1`，页面提示 `Done`。
  2. B 关闭管理面板、重新进入 Contacts；C 加入组织后打开 Contacts。
  3. B 移除 C，最后删除整个组织；观察 B、C、E 已打开的组织详情。
- 期望：写操作成功后，发起端的联系人卡片、面包屑、标题和管理面板立即更新；移除或删除后关闭失效详情。
- 实际：C 首次拉取即显示新名称 `QA组织-20260726-R1`，证明重命名已生效；B 的联系人卡片、详情和管理面板始终显示旧名称。C 被移除、组织被删除后，通讯录入口虽消失，但各端已打开的组织详情和操作面板仍保留，随后出现 `Failed to load org: not an org member`。
- 复现率：重命名 1/1；移除 1/1；删除 1/1。
- 影响：组织管理者和成员可能基于陈旧名称、成员和权限状态继续操作，页面与服务端事实不一致。
- 初步定位：组织写操作后的本地 DataGateway 更新与当前 `org-view` / 管理弹层失效处理不完整。

### BUG-006 [P2] 普通或已撤权的组织成员仍显示完整管理面板

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`。
- 账号：B `qa260726b7421`（组织创建者），C `qa260726c7421`、E `qa260726e7421`（普通成员）。
- 前置条件：C、E 已加入组织，未被授权管理员。
- 步骤：
  1. C 或 E 打开组织详情。
  2. 观察并点击 `Manage`。
  3. C 在普通成员状态尝试添加 E。
  4. B 授予 C 管理员，C 成功添加 E；B 再撤销 C，C 在仍打开的面板尝试添加 A。
- 期望：普通成员只读浏览且不显示 `Manage`；撤权后入口和面板立即消失。
- 实际：普通成员可打开包含 Rename、Delete organization、New department、成员 Rank/Remove、Add member、Grant admin 等全部按钮的管理面板。服务端会以 `not authorized to manage this org node` 拒绝普通或撤权后的实际写操作，未发生越权数据修改，但 UI 不反映权限。
- 复现率：普通成员 C、E 均显示管理入口，2/2；撤权后 C 面板仍保留，1/1。
- 影响：严重误导用户并暴露不应展示的破坏性操作；所有权限保护被迫依赖服务端最后一道校验。
- 初步定位：组织详情无基于当前用户管理员状态的入口与按钮渲染控制，权限变更通知也未关闭已有管理弹层。

### BUG-007 [P2] 用户昵称和头像更新不增量同步到其他在线账号

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B、E 同时在线。
- 账号：B `qa260726b7421`，E `qa260726e7421`。
- 前置条件：B、E 已互为好友且存在单聊、共同群聊。
- 步骤：
  1. E 在 Settings 将昵称从 `QA压力E` 改为 `QA压力E-R1`，页面提示 `Profile updated`。
  2. E 上传 `test-image.png` 作为头像，页面提示 `Avatar updated`。
  3. E 修改密码并用新密码重新登录，Settings 仍显示新昵称，证明资料已保存。
  4. E 向 B 发送 `PROFILE-E-R1-message-008`；B 等待后重新打开 Friends 和 Chat。
- 期望：B 收到资料变更后更新好友卡片、会话名、群成员名和头像。
- 实际：B 收到新消息，但好友、会话和群成员仍显示 `QA压力E` 及字母 `Q` 头像；普通页签切换和等待均不更新。
- 复现率：昵称 1/1；头像 1/1。
- 影响：联系人身份展示长期陈旧，用户无法判断对方最新资料。
- 初步定位：用户 profile 变更后缺少对相关联系人会话的轻通知，或 SDK 收到通知后未主动补拉并使视图失效。

### BUG-008 [P1] 切换会话后右侧详情仍属于上一个会话

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；Desktop 三栏布局。
- 账号：B `qa260726b7421`。
- 前置条件：B 在群聊 `QA群聊-20260726` 打开 Details，右侧显示群头像、群设置和 4 名成员。
- 步骤：
  1. 保持群详情打开，在全局搜索输入 `PROFILE-E-R1-message`。
  2. 点击命中的 E 单聊记录。
  3. 观察中间聊天标题和右侧详情。
- 期望：切换到 E 单聊时，右侧详情同步切换为 E 的用户名、备注、免打扰和屏蔽设置，或自动关闭。
- 实际：中间标题已变为 `E-备注-R1` 并显示 E 的单聊消息；右侧仍显示 `QA群聊-20260726`、Favorite Group、Add Member、4 members 和群主移人按钮。
- 复现率：1/1。
- 影响：三栏同时展示两个不同会话的操作，用户可能在单聊语境下误改群设置或移除群成员。
- 初步定位：全局搜索和会话切换只更新 active conversation，未使已打开的 detail panel 按新 target 重新渲染。

### BUG-009 [P2] 转发卡片标注可点击但无法查看转发内容

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`。
- 账号：B `qa260726b7421`（转发者），A `qa260726a7421`（接收者）。
- 前置条件：B 与 E 的单聊中存在 `PROFILE-E-R1-message-008` 和 `UNBLOCKED-E-to-B-success-010`。
- 步骤：
  1. B 将一条消息转发到 A 单聊和 `QA群聊-20260726`，备注 `forward-comment-011`。
  2. B 再多选两条消息合并转发到群聊，备注 `multi-forward-012`。
  3. A 点击 `Forwarded 1 items (click to view)` 和对应消息气泡。
- 期望：打开转发详情，展示被转发的原始消息内容、顺序和发送者。
- 实际：转发发送和各端通知成功，卡片分别显示 1/2 items 和备注；点击文字及整个消息气泡均无任何反应，原始内容不可查看。
- 复现率：单条 1/1；多条 1/1。
- 影响：转发消息只剩数量和备注，核心内容无法读取，转发功能对接收者基本不可用。
- 初步定位：`packages/uikit/src/app/views/chat/message-list.ts` 只渲染 `.message-forward-block` 文案，没有注册点击处理或转发详情视图。

### BUG-010 [P2] 点击引用消息不跳转原消息，只重复展开同一预览

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`。
- 账号：E `qa260726e7421`。
- 前置条件：E 在群聊中引用 `GROUP-after-D-remove-006`，发送 `group-quote-reply-014`。
- 步骤：点击引用回复中的 `GROUP-after-D-remove-006` 引用块。
- 期望：滚动到原消息并短暂高亮，或展示比截断预览更完整的原消息详情。
- 实际：页面不滚动、不高亮原消息，只在引用块内部新增一行完全相同的 `GROUP-after-D-remove-006` 文本；再次点击折叠。
- 复现率：1/1；单聊和群聊共用同一引用渲染逻辑。
- 影响：长聊天中无法定位引用上下文，点击后的重复文本没有信息增量。
- 初步定位：`packages/uikit/src/app/views/chat/message-list.ts` 的引用点击只切换 `expandedQuoteMessageIds` 并重复渲染 `quote.preview`，没有查找原 `msg_id`、滚动或高亮。

### BUG-011 [P2] 群头像更新不增量同步到已在线成员

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 为群主，A、E 已在线。
- 账号：A `qa260726a7421`，B `qa260726b7421`，E `qa260726e7421`。
- 前置条件：B 在新登录页面打开群详情，A、E 已打开群聊或群详情。
- 步骤：B 上传 `test-image.png` 作为群头像，等待后让 E 关闭并重新打开 Details。
- 期望：A、E 的会话卡片和群详情同步显示新头像。
- 实际：B 提示 `Group avatar updated` 并立即显示图片；A、E 等待以及重新打开 Details 后仍显示字母 `Q`。新登录的 B 页面可从服务端拉到图片，说明保存成功。
- 复现率：1/1。
- 影响：群资料在多端长期不一致。
- 初步定位：群头像更新后的通知或 SDK 群资料补拉未使现有会话卡片和详情失效。

### BUG-012 [P1] 离开 Chat 时收到的新消息只更新预览，返回后正文不追平

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 同账号两个页面。
- 账号：B `qa260726b7421`（页面 1、页面 2），E `qa260726e7421`。
- 前置条件：B 两页均已打开同一群；页面 1 停留群聊，页面 2 切到 Settings。
- 步骤：
  1. E 向群内发送 `SYNC-READ-cross-page-017`。
  2. 页面 1 立即收到并显示正文。
  3. 页面 2 返回 Chat，等待 2.5 秒。
  4. 页面 2先切到另一会话，再切回群聊。
- 期望：页面 2 返回 Chat 时当前群消息列表立即补拉到与会话预览相同的最新消息。
- 实际：页面 2 的会话预览已显示 `SYNC-READ-cross-page-017`，但当前群正文仍停在 `SYNC-B1-to-B2-016` 并显示 `Reached latest messages`；只有切换到另一会话再切回后正文才出现。
- 复现率：1/1。
- 影响：用户看到会话预览有新消息，却在正文中找不到，可能误判消息丢失。
- 初步定位：从非 Chat 视图返回时复用旧 active conversation 的内存消息列表，没有根据预览 seq 变化触发当前会话补拉。

### BUG-013 [P1] 同 token 在一个页面退出后，其他页面的 WebSocket 仍可发信但 HTTP 上传变为 401

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；同一浏览器上下文的两个 E 页面。
- 账号：E `qa260726e7421`。
- 前置条件：原 E 页面已登录并保持 WebSocket 在线；新页面继承同一 E 登录状态。
- 步骤：
  1. 新页面点击 Logout，返回登录页。
  2. 原 E 页面不刷新，继续通过聊天输入框发送 `SYNC-READ-cross-page-017`。
  3. 原 E 页面选择图片上传；以 200 ms 间隔观察占位和错误。
- 期望：同 token 退出后，其他页面立即收到会话失效并回到登录页；或者该页面所有通道继续有效，不能出现协议分裂。
- 实际：原 E 页面仍可通过既有 WebSocket 成功发送群消息；图片先出现 `Sending…`，约 400 ms 后消失并显示 `Upload failed: upload failed: HTTP 401`。页面不会自动退出。
- 复现率：E 1/1；更早的 A 多页面流程出现相同 HTTP 401，合计 2 个账号。
- 影响：注销后的 WebSocket 会话仍能执行写操作，存在会话撤销不完整的安全风险；同一页面文本可发、附件不可发，用户状态自相矛盾。
- 初步定位：logout 撤销 Bearer token 后未主动关闭或使服务端拒绝使用该 token 建立的现有 WebSocket，前端也未在上传 401 后统一失效当前 session。

### BUG-014 [P1] 多页面登录不同账号后，刷新会切换成最后登录账号并混用页面模式

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；同一无痕浏览器上下文的 B、D 页面。
- 账号：B `qa260726b7421`、D `qa260726d7421`。
- 前置条件：B 的第二页面以 Instant 模式登录并显示 B 与 E 的 220 条单聊；D 页面随后退出并重新登录 D。
- 步骤：
  1. 确认 B 页面会话标题为 `QA压力E-R1`，正文显示 `LOAD-D-192` 至 `LOAD-D-220`，页面身份为 B。
  2. 在 D 页面执行 Logout，再使用 D 账号登录。
  3. 刷新原 B 页面并等待初始化。
  4. 打开刷新后页面的 Settings，检查昵称、UID 和存储模式。
- 期望：每个页面保持自己的登录账号；刷新 B 页面后仍是 B，或明确要求重新登录，绝不能静默切换成 D。
- 实际：原 B 页面刷新后只显示 D 的群会话和退群系统消息；Settings 显示昵称 `QA访客D`、UID `356767686782357504`，但存储模式仍是原 B 页的 `Instant`，形成 D 身份与 B 页面模式的混合状态。
- 复现率：1/1。
- 影响：多页面测试和真实用户的多账号场景会发生身份串号；用户可能在未察觉的情况下以另一账号读写消息，属于高风险会话隔离问题。
- 初步定位：认证 token 由同源共享存储保存，而页面运行时账号和存储模式又各自保留；初始化时没有校验 token 身份与当前页面 session 配置是否一致。

### BUG-015 [P2] 新登录页面短暂把对端单聊渲染成自己的空白会话

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 新登录页面。
- 账号：B `qa260726b7421`、E `qa260726e7421`。
- 前置条件：B 与 E 已有单聊和历史消息，B 在新页面重新登录。
- 步骤：
  1. 登录 B 后等待初始化完成并查看会话列表。
  2. 点击带有 E 最新消息预览的单聊会话。
  3. 观察会话数据键、标题和正文，继续等待资料同步。
- 期望：会话从首次可交互开始就以 E 的 UID、昵称和头像显示，并加载 B 与 E 的历史消息。
- 实际：会话最初使用 B 自己的 UID 和昵称 `QA成员B`，点击后打开标题为自己的空白单聊；稍后资料同步完成后才自行修正为 E 的 UID、昵称 `QA压力E-R1` 和头像。
- 复现率：1/1。
- 影响：用户在刚登录时可能误以为历史消息丢失或正在与自己聊天，也可能误点后向错误的会话目标操作。
- 初步定位：Instant 初始化阶段先用当前用户资料占位对端会话，联系人 / 会话资料补拉完成后才纠正 target。

### BUG-016 [P2] 同账号另一在线页面不会自动同步好友备注变更

- 环境：`https://yimsg.im/app/`，代码基线 `2f1961d`；B 同账号两个 Instant 页面。
- 账号：B `qa260726b7421`、好友 A `qa260726a7421`。
- 前置条件：B 的两个页面均在线并打开 Friends，A 在两页都显示为 `QA群主A`。
- 步骤：
  1. B 页面 1 打开 A 的好友详情，设置备注 `A-同步备注-R2` 并保存。
  2. 页面 1 等待约 1.5 秒，列表更新为新备注。
  3. 页面 2 保持 Friends 并继续等待约 3.6 秒。
  4. 页面 2 手动切到 Requests，再切回 Friends。
- 期望：页面 2 收到轻通知后主动拉取联系人变更，无需用户切换页签。
- 实际：页面 2 等待后仍显示 `QA群主A`；只有手动切换页签触发拉取后才显示 `A-同步备注-R2`。
- 复现率：1/1。
- 影响：同账号多页面的联系人资料长期不一致，用户可能按旧备注识别联系人。
- 初步定位：好友备注写入后的联系人通知未触发另一 session 的 Contacts 列表失效 / 补拉，页签切换路径才执行刷新。

### BUG-017 [P2] 长会话在网络延迟下通过全局搜索跳转后，高亮会被自动续拉重渲染清除

- 环境：本地基线 `2f1961d`，`./tools/run_all_tests.sh` 的 Playwright Chromium UI 阶段；测试显式模拟 400 ms 延迟、750 Kbps 下载和 250 Kbps 上传。
- 前置条件：目标单聊中先发送命中消息，再发送 34 条后续消息，使目标位于长会话非末尾并触发 around 拉取后的自动续页。
- 步骤：
  1. 从另一单聊使用全局搜索查找目标消息 `apple pie`。
  2. 点击结果跨会话跳转。
  3. 观察 `.message-row.msg-highlight` 在 1.5 秒高亮窗口内是否持续存在。
- 期望：目标消息高亮在整个窗口内保持可见，后续自动续页和消息列表重渲染不能清除它。
- 实际：初次运行高亮曾出现但轮询时数量从 1 变为 0；重试时高亮节点最终消失，无法再定位目标气泡。初次和重试均失败。
- 复现率：全量测试 2/2（初次 + retry）；使用同一 Chromium 路径改为单 worker 独立复跑时 0/1，说明问题与并发负载 / 时序有关，但全量门禁仍稳定失败。
- 影响：长历史或较慢网络下，用户从全局搜索跳转后可能瞬间失去定位，不知道命中的是哪条消息。
- 证据：全量测试失败断言位于 `apps/web/tests/ui/chat.spec.ts:128` 和 `:131`；初次高亮数量从 1 变 0，retry 最终找不到高亮消息气泡。
- 初步定位：around 窗口触边后自动续拉会整体重渲染消息列表，高亮 class 只临时附着于旧 DOM，没有作为可恢复状态在重渲染后重新应用。

后续 Bug 使用以下格式：

```text
BUG-001 [P1/P2/P3] 标题
环境：
账号：
前置条件：
步骤：
期望：
实际：
复现率：
证据：
代码定位（可选）：
```

## 8. 本轮观察日志

| 时间（Asia/Shanghai） | 记录 |
|---|---|
| 2026-07-26 | 登录页可见 Login、Register、用户名、密码、Instant、Persistent、Auto、Desktop、Mobile。 |
| 2026-07-26 | 本地 `main` 从 `3fba9fc` 快进到 `2f1961d`，新增搜索、群成员管理、@ 提及和发送中占位相关功能，已纳入清单。 |
| 2026-07-26 | 服务端完成重新部署并清空数据；此前账号、UID和连接异常全部作废，不计入结果。 |
| 2026-07-26 | 重建账号：A `356766549220003840`（Persistent/Desktop）、B `356767044860907520`（Instant/Auto）、C `356767347920343040`（Instant/Auto）、D `356767686782357504`（Persistent/Mobile）、E `356768224206917632`（Instant/Desktop）。 |
| 2026-07-26 | 五个页面首次并发在线时各自保持内存身份；后续刷新暴露共享认证状态串号，详见 BUG-014。 |
| 2026-07-26 | 多页面在不刷新时可保持不同内存身份；D 重新登录后刷新原 B 页面，页面静默切换为 D，同时保留原页 Instant 模式，记录为 BUG-014。 |
| 2026-07-26 | 压力测试完成：群聊 120 条、单聊 220 条均连续有序且无重复，单聊覆盖 150 条窗口双向分页，另验证约 2200 字符及 HTML / script 文本转义。 |
| 2026-07-26 | B 页面 1 修改 A 的好友备注后，B 页面 2 不自动更新；切换 Requests / Friends 才拉到新值，记录为 BUG-016。 |
| 2026-07-26 | 全量测试除 Playwright UI 外均通过；UI 为 134 passed、1 failed、2 flaky，稳定失败是长会话延迟下全局搜索高亮被续拉重渲染清除，记录为 BUG-017。 |
| 2026-07-26 | BUG-017 使用单 worker 独立复跑通过（1 passed，24.5 秒）；保留为并发负载 / 时序缺陷，完整门禁结果仍按失败记录。 |
