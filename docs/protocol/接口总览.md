# 接口总览

> 主要对照：`frontend/src/sdk/index.ts`、`frontend/src/sdk/client.ts`、`frontend/src/sdk/generated/actions.gen.ts`、`frontend/src/sdk/internal/action-mappers.ts`、`internal/ws/connection.go`、`internal/protocol/`、`cmd/server/main.go`。
> 最后复核：2026-07-16。
> 触发更新：WebSocket action、HTTP 上传 / 静态资源接口、请求 / 响应字段、错误码、SDK ↔ 服务端映射或 `client_config` 变化时同步更新。
> 入口关系：上级索引见 [`../README.md`](../README.md)；协议治理与帧格式见 [`README.md`](README.md)；type / 字段速查表见 [`../generated/协议接口表.md`](../generated/协议接口表.md)；SDK 公开 API 权威文档见 [`../frontend/sdk/sdk接口说明.md`](../frontend/sdk/sdk接口说明.md)；UIKit 嵌入权威文档见 [`../frontend/uikit/UIKit方案.md`](../frontend/uikit/UIKit方案.md)；同步机制见 [`../同步机制方案.md`](../同步机制方案.md)。

本文是跨层接口契约的权威总览：负责分层规范、命名规则、SDK ↔ 服务端映射和服务端接口矩阵。各层接口的完整调用说明不在本文重复维护：UIKit 挂载 API 见 [`../frontend/uikit/UIKit方案.md`](../frontend/uikit/UIKit方案.md)，SDK 方法 / 事件 / 类型见 [`../frontend/sdk/sdk接口说明.md`](../frontend/sdk/sdk接口说明.md)。

## 目录

- [1. 规范](#1-规范)
- [2. SDK 与服务端接口映射](#2-sdk-与服务端接口映射)
- [3. 服务端接口详解](#3-服务端接口详解)
- [附录：新增接口落地清单](#附录新增接口落地清单)

---

## 1. 规范

### 1.1 分层边界

Yimsg 的公开调用面分三层：

| 层级 | 入口 | 主要调用者 | 职责 | 权威文档 |
|---|---|---|---|---|
| UIKit | `mount()`、`MountHandle`、widget 事件 | 第三方页面、项目主应用、九宫格控制台 | 提供完整聊天 UI，隐藏业务同步和 DOM 细节 | [`UIKit方案.md`](../frontend/uikit/UIKit方案.md) |
| SDK | `YimsgClient` | 自定义 UI、UIKit、自动化场景 | 管理 WebSocket、认证、会话、分页读取、同步、缓存和事件 | [`sdk接口说明.md`](../frontend/sdk/sdk接口说明.md) |
| 服务端 | WebSocket protobuf Type、`POST /api/upload`、`GET /media/*`、静态资源 | SDK DataGateway / transport、浏览器或宿主页面 | 执行业务校验、写入分片存储、返回 protobuf 定义的响应、发送轻量通知和提供资源访问 | 本文 §3 |

UIKit 不直接访问服务端 action；UIKit 只使用 `YimsgClient`。业务方自定义 UI 时，也应优先使用 SDK 门面，而不是自己拼 WebSocket 包。

### 1.2 设计理念

| 原则 | 说明 |
|---|---|
| WebSocket protobuf 帧为主接口 | 注册、登录、消息、联系人、群组、会话偏好都走 WebSocket interface；HTTP 只用于上传、媒体访问和静态资源 |
| protobuf 单一事实源 | `internal/protocol/yimsg.proto` 定义 type、request、response、BaseResponse、ErrorCode 和 notification |
| 轻通知 + 主动拉取 | 服务端通知只表达“有变化”；客户端收到后用分页读取、游标同步或单点查询追平 |
| 分页优先 | 会话、消息、联系人、屏蔽列表、免打扰和群成员都按分页读取，避免无界集合进入 JS 堆 |
| SDK 统一同步 | instant 与 persistent 模式差异由 SDK DataGateway 屏蔽，UI 只消费 SDK 的只读事件和分页方法 |
| 应用层聚合 | 服务端按 `uid`、`username`、`group_id`、`token` 四类路由键分片；跨表、跨分片聚合在应用层完成，避免 JOIN |
| 鉴权先注册在线态 | `login` / `authenticate` 成功时，服务端先把连接注册到在线表，再返回成功响应，避免紧随其后的推送丢失 |

### 1.3 命名规范

**函数 / 方法：**

| 层 | 命名风格 | 示例 |
|---|---|---|
| UIKit 挂载函数 | camelCase | `mount`、`openConversation`、`setTheme` |
| SDK 公开方法 | camelCase | `sendMessage`、`getConversations`、`favoriteGroup` |
| SDK 事件名 | domain:event | `messages:received`、`contacts:updated` |
| SDK DataGateway 读取方法 | snake_case， 对齐 action； 同步 action 只作为持久存储内部实现， 不进入 DataGateway 接口 | `get_messages`、`get_contacts` |
| WebSocket action | snake_case，动词 + 对象；由 protobuf `Type` 注释映射到 type | `send_message`、`update_group_info` |
| Go Service 导出方法 | PascalCase | `SendMessage`、`ListByConversation` |
| Go 局部变量 / 参数 | camelCase | `friendUID`、`groupID`、`lastSeq` |

**字段：**

| 场景 | 规则 | 示例 |
|---|---|---|
| SDK 入参 / 返回字段 | camelCase | `toUid`、`groupId`、`cursor` |
| WebSocket protobuf 字段 | snake_case；protobuf wire 字段 | `target`、`group_id`、`page` |
| Go 协议结构字段 | PascalCase + JSON tag | `ToUID json.Number \`json:"to_uid,omitempty"\`` |
| 数据库列名 | snake_case | `friend_uid`、`sort_key`、`search_text` |
| 批量字段 | 复数 | `uids`、`group_ids`、`member_uids` |
| 同步游标字段（`sync_*`） | 统一词表 | `last_seq`、`limit`、`rebuild` |
| 展示分页字段（`get_*`） | 不透明 keyset 游标 | `page.cursor`、`page.direction`、`page.limit`、`page.around` |

**action 命名细则：**

- 读：`get_*` / `sync_*`
- 写：`send_*` / `mark_*` / `add_*` / `remove_*` / `update_*` / `block_*` / `unblock_*`
- 会话目标统一使用 `ConversationTarget`：私聊为 `{ uid }`，群聊为 `{ group_id }`。

新增接口必须显式完成 `camelCase ↔ snake_case` 映射，禁止跨层透传未转换字段。

### 1.4 协议与认证约束

**帧与请求 / 响应基础字段：**

- WebSocket 帧格式固定为 `magic:uint8('M') + codec:uint8(bitfield) + reserved:uint8(0) + checksum:uint8(CRC-8) + size:uint16 + request_id:uint64 + type:uint16 + body`；各字段语义、CRC-8 参数、整包上限和解码顺序的权威说明见 [`README.md`](README.md)，本文不重复维护。
- 每个接口有独立 Request / Response。`uid` 与 `request_id` 从帧头解析后由服务端框架填入 `BaseInfo` 结构体传入业务方法，proto 中不再包含 `BaseRequest`。Response 使用 `BaseResponse base = 1` 承载 `ErrorCode code` 和 `msg`，业务字段从 protobuf 字段号 10 开始，不能在每个响应里重复定义 `code` / `msg`。

**认证：**

- 免认证 action 只有 `register`、`login`、`authenticate`；其余 action 都要求连接已认证。
- 当前 `uid` 由服务端连接状态写入 `BaseInfo`，客户端无需也不应传自己的 `uid`。
- `login` / `authenticate` 成功时，服务端先完成在线注册，再发送成功响应，并在响应发出后启动该连接的推送转发。

**数值编码：**

`uid`、`friend_uid`、`to_uid`、`group_id` 等 int64 字段在 JSON 中按字符串传输；SDK 对外 ID 类型同样使用字符串，避免前端数字精度丢失。

`msg_id` 是原生 string 类型（不是 int64）：UUIDv7 的 base64url 编码，固定 22 字符。整个项目（protobuf / Go / TypeScript / SQLite / JSON / WebSocket）msg_id 一律 string，禁止任何二进制 UUID 表示。用户消息的 msg_id 只由 TypeScript SDK 生成并随 `send_message` 上送，服务端只做校验、保存、回传与幂等；缺失或非法直接拒绝；系统消息由服务端发起、无 SDK 来源，是唯一允许由服务端生成 msg_id 的消息类型。消息顺序只依赖 `seq` / `send_time`，不得以 `msg_id` 排序。

**分页与同步：**

- `limit` 默认 200，并受服务端 `[client].batch_max_limit` 裁剪；硬上限为 500。
- `get_*` 用于当前页 / 管理页读取；`sync_*` 用于 DataGateway 内部按游标追平变化；UI 不应把 `sync_*` 当成全量列表接口。
- tombstone、GC 水位和 `seq_too_old` 的恢复规则统一见 [`../同步机制方案.md`](../同步机制方案.md)。

### 1.5 `client_config`

`login` / `authenticate` 成功响应会返回 `client_config`：

| 字段 | 默认值 | 说明 |
|---|---:|---|
| `cache_ttl_seconds` | `604800` | 客户端显示信息缓存 TTL，默认 7 天 |
| `cache_max_entries` | `10000` | 客户端显示信息缓存最大条目数，用户和群合计计算 |
| `recall_window_seconds` | `120` | 撤回时限；`< 0` 表示显式禁用 |
| `batch_max_limit` | `500` | 服务端分页 / 批量接口和 SDK 单批网络请求上限，硬封顶 500 |

SDK 中对应 `ClientConfig` 字段为 `cacheTtlSeconds`、`cacheMaxEntries`、`recallWindowsSeconds` 和 `batchMaxLimit`；显示信息缓存 TTL / 最大条目数只使用构造参数或 SDK 默认值，认证后不运行期改变；`recallWindowsSeconds` 以后端返回为准，`batchMaxLimit` 同时存在前端构造参数和后端 `client_config.batch_max_limit` 时取较小值。调用方可通过 `getClientConfig()` 读取当前生效配置。

---

## 2. SDK 与服务端接口映射

### 2.1 SDK ↔ 服务端 action 映射

下表按调用者视角维护 SDK 公开方法、SDK 内部协议动作与服务端 WebSocket type / HTTP 路由的对应关系。没有对应侧时显式写“无”；`sync_*` type 只由 SDK DataGateway 内部使用，不通过 SDK 门面对外暴露。各 SDK 方法的签名、前置条件和返回类型见 [`sdk接口说明.md`](../frontend/sdk/sdk接口说明.md)。

| SDK / 客户端入口 | 服务端接口 | 关键参数 | 说明 |
|---|---|---|---|
| `new YimsgClient` | `GET /ws` | SDK: `ClientOptions`；服务端: WebSocket Upgrade | 创建 UI 无关 SDK 客户端并按需建立 WebSocket 通道 |
| `YimsgClient.estimateMaxMemoryBytes` | 无 | `options?: ClientOptions` | 静态估算 SDK 配置对应的 JS 堆内存上界 |
| `getSessionSnapshot` / `getClientConfig` / `startSession` / `destroy` | 无 | `SessionStartOptions` | 管理 SDK 本地生命周期、配置快照、DataGateway 和资源释放 |
| `on` / `off` / `once` / `listenerCount` / `removeAllListeners` | 无 | `event`、`handler?` | 管理 SDK 本地事件监听器 |
| `register` / `login` / `authenticate` / `logout` | `register` / `login` / `authenticate` / `logout` | `username`、`password`、`nickname`、`token` | 注册、登录、token 鉴权和登出 |
| `updateUserInfo` / `updatePassword` | `update_user_info` / `update_password` | `nickname?`、`avatarUrl?`、`oldPassword`、`newPassword` | 修改个人资料或密码 |
| `searchUser` / `getUserInfos` / `getGroupInfos` / `getOrgInfos` / `getTagInfos` | `search_user` / `get_user_infos` / `get_group_infos` / `get_org_infos` / `get_tag_infos` | `username`、`uids`、`groupIds`、`orgIds`、`orgId, tagIds` | 搜索用户、批量读取并缓存用户 / 群 / 组织 / tag 展示资料（四者共用 `DisplayInfoCache`：同步读缓存、TTL 过期后台刷新） |
| `getContacts` / `getContactCount` | `get_contacts` / `get_contact_count` | `cursor?`、`backward?`、`around?`、`limit?`、`status`、`friendUid?`、`groupId?`、`orgId?`、`friendUids?`、`groupIds?`、`orgIds?` | 分页读取通讯录、按状态计数（待我处理传 `CONTACT_STATUS_PENDING_INCOMING`，我发出待对方处理传 `CONTACT_STATUS_PENDING_OUTGOING`，好友/收藏群/组织传 `CONTACT_STATUS_FRIEND`）；传 `friendUid`/`groupId`/`orgId` 且 `limit=1` 可替代旧联系人状态判断 |
| `addFriend` / `acceptFriend` / `rejectFriend` / `deleteFriend` / `updateRemark` / `favoriteGroup` / `unfavoriteGroup` | `add_friend` / `accept_friend` / `reject_friend` / `delete_friend` / `update_remark` / `favorite_group` / `unfavorite_group` | `friendUid?`、`groupId?`、`remarkName?` | 管理好友关系、备注和群收藏；`acceptFriend`/`rejectFriend` 只有请求接收方能调用成功，申请方对自己发出的请求调用会返回 `CONFLICT` |
| `blockUser` / `unblockUser` / `getBlocklist` | `block_user` / `unblock_user` / `get_blocklist` | `uid`、`uids?`、`cursor?`、`backward?`、`limit?`、`status?` | 管理和查询屏蔽列表状态 |
| `getConversations` / `getUnreadCount` / `clearUnread` / `deleteConversation` | `get_conversations` / `get_unread_count` / `clear_unread` / `delete_conversation` | `cursor?`、`backward?`、`limit?`、`toUid?`、`groupId?` | 分页读取会话、读取未读总数、清理未读和删除会话 |
| `sendMessage` / `sendText` / `sendMarkdown` / `sendImage` / `sendFile` / `sendQuotedTextMessage` / `forwardMessages` | `send_message` | `target`、`body`、`msgType?` | 发送强类型 `MessageBody` 消息；图片/文件先上传得到 `media_id` 再引用 |
| `getMessages` / `recallMessage` / `deleteMessage` | `get_messages` / `send_message`(`MESSAGE_TYPE_RECALL`) / `delete_message` | `target`、`cursor?`、`backward?`、`around?`、`limit?`、`msgId` | 分页读取、撤回（走 send_message + RecallBody）和删除消息 |
| `describeConversation` / `describeMessageConversation` / `describeMessage` / `validateTextMessage` | 无 | `ConversationTarget`、`LocalConversation`、`Message` | 本地解释会话 / 消息、从强类型 body 派生展示、校验文本长度 |
| `muteConversation` / `unmuteConversation` / `getMutelist` | `mute_conversation` / `unmute_conversation` / `get_mutelist` | `target`、`cursor?`、`backward?`、`limit?`、`toUid?`、`groupId?`、`toUids?`、`groupIds?`、`status?` | 设置、取消、分页读取和过滤查询会话免打扰 |
| `createGroup` / `getGroupMembers` / `updateGroupInfo` / `addGroupMember` / `removeGroupMember` | `create_group` / `get_group_members` / `update_group_info` / `add_group_member` / `remove_group_member` | `name`、`memberUids`、`groupId`、`uid`、`avatarUrl?`、`cursor?`、`backward?`、`limit?` | 创建群、读取成员、更新群资料和管理成员 |
| `uploadFile` | `POST /api/upload` | `file`、`category`、Bearer token | 上传头像、图片或文件 |
| `formatTime` / `formatFileSize` / `displayUserName` / `displayGroupName` | 无 | `timestamp`、`bytes`、展示信息对象 | SDK 纯工具函数，不触发网络请求 |
| `getTags` | `get_tags` | `orgId`、`tagId`、`cursor?`、`backward?`、`limit?` | 展开 tags（组织关系表）某节点的直接子项（展开组织根传 `tagId=orgId`）；子项名字不内嵌，另调 `getTagInfos`/`getUserInfos` 补齐 |
| `createOrg` | `create_org` | `name`、`avatar?` | 创建组织，任意登录用户可调用——权限链条唯一的自举点；调用方自动成为新组织根的初始管理员 |
| `createOrgTag` / `renameOrgTag` / `deleteOrgTag` / `linkOrgTag` | `create_org_tag` / `rename_org_tag` / `delete_org_tag` / `link_org_tag` | `orgId`、`parentTagId?`、`tagId?`、`childTagId?`、`name?`、`avatar?`、`rank?` | 创建、重命名、删除 tag（部门/横向分组），或把已存在的 tag 额外挂到另一个父节点下（DAG 多父）；均需对目标节点有管理权限 |
| `addOrgMember` / `addOrgMemberByUsername` / `removeOrgMember` | `add_org_member` / `remove_org_member` | `orgId`、`tagId`、`uid`/`username`、`title?`、`rank?` | 把人挂进或移出某个组织节点；`addOrgMemberByUsername` 是按用户名录入的界面层入口，内部解析出 uid 后落地到 `addOrgMember`；均需对该节点有管理权限 |
| `setOrgItemRank` | `set_org_item_rank` | `orgId`、`tagId`、`childId`、`childType`、`rank`、`title?` | 调整某条边的排序与职务展示；需对该边所属父节点有管理权限 |
| `renameOrg` / `deleteOrg` | `rename_org` / `delete_org` | `orgId`、`name?`、`avatar?` | 重命名组织展示资料，或删除整个组织（结构同步清空，成员通讯录组织行异步清理）；均需对组织根有管理权限 |
| `grantOrgAdmin` / `grantOrgAdminByUsername` / `revokeOrgAdmin` / `listOrgAdmins` | `grant_org_admin` / `revoke_org_admin` / `list_org_admins` | `orgId`、`scopeTagId`、`uid`/`username` | 授予、撤销或列出某节点为根子树的管理员授权；`grantOrgAdminByUsername` 是按用户名录入的界面层入口，内部解析出 uid 后落地到 `grantOrgAdmin`；均需调用方自己已对该节点有管理权限，组织根撤权保证至少保留一个根管理员 |
| SDK DataGateway 内部（仅持久存储模式） | `sync_contacts` / `sync_conversations` / `sync_messages` / `sync_blocklist` / `sync_mutelist` / `sync_tags` | `last_seq`、`limit?` | 维护持久存储本地副本，不作为 SDK 门面暴露；instant 模式不调用 `sync_*`；`sync_tags` 按 org_id 一游标 |
| SDK transport 内部心跳 `ping` | `ping` | 无 | 已认证连接的轻量心跳探测 |
| 无 | `GET /media/*` / `GET /app/*` / `GET /demo/*` / `GET /uikit/*` | 媒体 URL 或静态文件路径 | 浏览器直接访问上传媒体文件和前端静态资源 |
| 无 | 插件自定义 type | 插件自定义参数 | 核心 dispatch 未命中时交给插件注册表处理 |

### 2.2 错误码

服务端失败响应通过 `BaseResponse.code/msg` 统一返回稳定错误码和可读文案。SDK 会兼容映射为 `RequestError.details.serverErrorCode`，调用方应优先基于 `serverErrorCode` 分支处理，避免依赖可读文案。

| error_code | 语义 |
|---|---|
| `INVALID_PROTOBUF` | protobuf body 不是合法 protobuf |
| `INVALID_FRAME` | WebSocket 二进制帧格式不合法 |
| `FRAME_TOO_LARGE` | 协议包总长度超过 64K 限制 |
| `AUTH_REQUIRED` | 当前 type 需要已认证连接 |
| `AUTH_FAILED` | 登录、鉴权或密码校验失败 |
| `UNKNOWN_ACTION` | 未注册的 WebSocket type 或 action 映射 |
| `INVALID_ARGUMENT` | 请求参数缺失、互斥或格式不合法 |
| `NOT_FOUND` | 目标用户、群、联系人、消息、会话或成员不存在 |
| `ALREADY_EXISTS` | 用户名、成员、状态等目标已存在 |
| `CONFLICT` | 当前资源状态与操作冲突 |
| `FORBIDDEN` | 当前用户无权限或业务规则不允许 |
| `SEQ_TOO_OLD` | 增量同步游标早于服务端 GC 安全水线 |
| `BATCH_LIMIT_EXCEEDED` | 批量请求去重后超过服务端上限 |
| `INTERNAL_ERROR` | 未分类的服务端内部错误 |

### 2.3 接口计数

| 类型 | 数量 | 范围 |
|---|---:|---|
| WebSocket type | 59 | `internal/protocol/yimsg.proto` 中核心 `Type`，不含插件自定义 type |
| HTTP 上传 | 1 | `POST /api/upload` |
| HTTP 媒体静态资源 | 1 | `GET /media/*` |
| HTTP 前端静态资源 | 1 | `GET /app/*` / `GET /demo/*` / `GET /uikit/*`，仅配置 `frontend.static_dir` 时启用 |
| 插件 type | 动态 | 核心 dispatch 未命中时交给插件注册表，约束见 [`../server/插件架构方案.md`](../server/插件架构方案.md) |

---

## 3. 服务端接口详解

服务端对外暴露三类接口：WebSocket protobuf Type（业务主接口）、HTTP 上传 / 静态资源、推送通知（单向下行）。WebSocket frame header 承载 `request_id` 和 `type`；body 使用 protobuf 定义的 request / response，失败状态在 `BaseResponse.code/msg` 中。逐 type 的 request / response 类型与字段以协议表 [`../generated/协议接口表.md`](../generated/协议接口表.md) 为准，本节仅保留 SDK 映射速查。通知类型、触发矩阵和客户端桥接的权威文档是 [`../server/推送事件方案.md`](../server/推送事件方案.md)，本文不重复维护。

### 3.1 WebSocket action 列表

| 模块 | Action | 功能说明 | SDK 入口 | 请求字段 | 响应字段 |
|---|---|---|---|---|---|
| 用户 | `register` | 注册新用户 | `register` | `username, password, nickname` | `uid` |
| 用户 | `login` | 登录并创建 session | `login` | `username, password` | `token, uid, client_config` |
| 用户 | `update_user_info` | 修改昵称 / 头像 | `updateUserInfo` | `nickname?, avatar?` | 空 |
| 用户 | `update_password` | 修改密码并踢下线所有设备 | `updatePassword` | `old_password, new_password` | 空 |
| 用户 | `search_user` | 按用户名搜索用户 | `searchUser` | `username` | `profile?` |
| 用户 | `get_user_infos` | 批量获取用户公开信息和备注 | `getUserInfos` | `uids[]` | `profiles[]`；去重后超限返回 `error_code=BATCH_LIMIT_EXCEEDED` |
| 登录态 | `authenticate` | token 鉴权并绑定连接 | `authenticate` | `token` | `uid, client_config` |
| 登录态 | `logout` | 删除当前 token session | `logout` | `token` | 空 |
| 登录态 | `ping` | 已认证连接心跳探测 | SDK transport 内部 | 空 | 空 |
| 通讯录 | `add_friend` | 发起好友请求 | `addFriend` | `friend_uid, remark_name?` | 空 |
| 通讯录 | `accept_friend` | 接受好友请求 | `acceptFriend` | `friend_uid` | 空 |
| 通讯录 | `reject_friend` | 拒绝好友请求 | `rejectFriend` | `friend_uid` | 空 |
| 通讯录 | `delete_friend` | 删除好友 | `deleteFriend` | `friend_uid` | `seq` |
| 通讯录 | `block_user` | 加入屏蔽列表 | `blockUser` | `uid` | `seq` |
| 通讯录 | `unblock_user` | 移出屏蔽列表 | `unblockUser` | `uid` | `seq` |
| 通讯录 | `get_blocklist` | keyset 游标分页读取屏蔽列表（新→旧），支持批量过滤 | `getBlocklist` | `page{cursor?,direction?,limit?}, uids?, status?` | `users[], page` |
| 通讯录 | `sync_blocklist` | 增量同步屏蔽列表 | DataGateway 内部 | `last_seq, limit?, rebuild?` | 成功 `users[], has_more, cursor_seq`；过旧游标返回 `ok=false, error_code=SEQ_TOO_OLD` |
| 通讯录 | `update_remark` | 修改好友或群收藏备注 | `updateRemark` | `target, remark_name` | 空 |
| 通讯录 | `get_contacts` | keyset 游标分页读取联系人（friend 按 sort_key、pending 按 seq 倒序），支持批量 targets 过滤 | `getContacts` | `page{cursor?,direction?,limit?,around?}, status?, targets?` | `contacts[], page`（展示总数改用 `get_contact_count`） |
| 通讯录 | `get_contact_count` | 按联系人状态统计数量 | `getContactCount` | `status`（必填非 0） | `total` |
| 通讯录 | `sync_contacts` | 增量同步联系人 | DataGateway 内部 | `last_seq, limit?, rebuild?` | 成功 `contacts[], has_more, cursor_seq`；过旧游标返回 `ok=false, error_code=SEQ_TOO_OLD` |
| 通讯录 | `favorite_group` | 收藏群到通讯录 | `favoriteGroup` | `group_id, remark_name?` | `seq` |
| 通讯录 | `unfavorite_group` | 取消群收藏 | `unfavoriteGroup` | `group_id` | `seq` |
| 消息 | `send_message` | 发送私聊或群聊消息（含撤回，撤回用 `msg_type=MESSAGE_TYPE_RECALL` + `body.recall`）；私聊发送方与接收方无需互为好友，只要未被拉黑即可发起临时会话 | `sendMessage` | `msg_id, target, msg_type, body` | `msg_id, seq` |
| 消息 | `delete_message` | 删除调用方消息 | `deleteMessage` | `msg_id` | `seq` |
| 消息 | `sync_messages` | 全局增量同步消息和删除 tombstone | DataGateway 内部 | `last_seq, limit?` | `messages[], has_more, cursor_seq`，`status=0xff` 表示删除 |
| 消息 | `get_conversations` | keyset 游标分页读取会话列表（活跃→沉默）；`targets` 非空时按目标定向读取当前状态、忽略分页 | `getConversations` | `page{cursor?,direction?,limit?}`、`targets[]` | `conversations[], page` |
| 消息 | `sync_conversations` | 增量同步服务端仍保留的会话变化 | DataGateway 内部 | `last_seq, limit?` | `conversations[], has_more, cursor_seq`，`status=0xff` 表示删除 |
| 消息 | `get_unread_count` | 获取全局未读总数 | `getUnreadCount` | 空 | `unread_count` |
| 消息 | `get_messages` | keyset 游标分页读取消息（旧→新；空游标+BACKWARD 取最新页；`page.around` 传 msg_id 居中定位），或按 `msg_ids` 批量读取 | `getMessages` | `target, page{cursor?,direction?,limit?,around?}, msg_ids?`（`msg_ids` 与 `page` 互斥，不分页、`page` 为空） | `messages[], page` |
| 消息 | `clear_unread` | 清零会话未读 | `clearUnread` | `target` | 空 |
| 消息 | `delete_conversation` | 删除调用方会话 | `deleteConversation` | `target` | `seq` |
| 消息 | `mute_conversation` | 开启免打扰 | `muteConversation` | `target` | `seq` |
| 消息 | `unmute_conversation` | 关闭免打扰 | `unmuteConversation` | `target` | `seq` |
| 消息 | `get_mutelist` | keyset 游标分页读取免打扰列表（新→旧），支持批量 targets 过滤 | `getMutelist` | `page{cursor?,direction?,limit?}, targets?, status?` | `mutes[], page`，当前有效项 `status=1` |
| 消息 | `sync_mutelist` | 增量同步免打扰 | DataGateway 内部 | `last_seq, limit?, rebuild?` | 成功 `mutes[], has_more, cursor_seq`，`status=0xff` 表示关闭；过旧游标返回 `ok=false, error_code=SEQ_TOO_OLD` |
| 群组 | `create_group` | 创建群并发送系统消息 | `createGroup` | `name, member_uids[]` | `group_id` |
| 群组 | `get_group_infos` | 批量获取群基本信息 | `getGroupInfos` | `group_ids[]` | `groups[]`；去重后超限返回 `error_code=BATCH_LIMIT_EXCEEDED` |
| 群组 | `get_group_members` | keyset 游标分页获取群成员（role 倒序、uid 升序） | `getGroupMembers` | `group_id, page{cursor?,direction?,limit?}` | `members[], page`（`page.total` 为成员总数） |
| 群组 | `update_group_info` | 修改群名称 / 头像 | `updateGroupInfo` | `group_id, name?, avatar?` | 空 |
| 群组 | `add_group_member` | 添加群成员并发送系统消息 | `addGroupMember` | `group_id, uid` | 空 |
| 群组 | `remove_group_member` | 移除群成员并发送系统消息 | `removeGroupMember` | `group_id, uid` | 空 |
| 组织 | `get_org_infos` | 批量读取组织展示资料字典（`org_info`，无 seq/status），顺带刷新调用方通讯录组织行投影 | `getOrgInfos` | `org_ids[]` | `orgs[]`；去重后超限返回 `error_code=BATCH_LIMIT_EXCEEDED` |
| 组织 | `get_tag_infos` | 批量读取 tag（部门/横向分组）展示资料字典（`tag_info`，无 seq/status，仅组织成员可读） | `getTagInfos` | `org_id, tag_ids[]` | `tags[]`；去重后超限返回 `error_code=BATCH_LIMIT_EXCEEDED` |
| 组织 | `get_tags` | keyset 游标分页展开 tags（组织关系表）某节点直接子项（tag 与人按 `rank, sort_key, child_type, child_id` 绝对排序混合；展开组织根传 `tag_id=org_id`，仅组织成员可读；子项不内嵌名字；GRANT 类型的管理员授权不出现在结果里） | `getTags` | `org_id, tag_id, page{cursor?,direction?,limit?}` | `tags[], page`（每项 `child_id, child_type, title, rank, sort_key, seq`） |
| 组织 | `sync_tags` | 按 org 增量同步 tags（组织关系表，含 tombstone；仅组织成员可读；GRANT 类型不出现在结果里） | DataGateway 内部 | `org_id, last_seq, limit?, rebuild?` | `tags[], has_more, cursor_seq`；过旧游标返回 `ok=false, error_code=SEQ_TOO_OLD` |
| 组织 | `create_org` | 创建组织，任意登录用户都可以调用——权限链条唯一的自举点；调用方自动成为新组织根的初始管理员（GRANT 边） | `createOrg` | `name, avatar?` | `org_id` |
| 组织管理 | `create_org_tag` | 创建 tag 并挂到父节点下；需对父节点有管理权限（GRANT 边覆盖，见组织架构方案第 10 节） | `createOrgTag` | `org_id, parent_tag_id, name, avatar?, rank?` | `tag_id` |
| 组织管理 | `rename_org_tag` | 重命名 tag 展示资料；需对该 tag 有管理权限 | `renameOrgTag` | `org_id, tag_id, name, avatar?` | 空 |
| 组织管理 | `delete_org_tag` | 删除 tag 及其两个方向的关联边（含挂在其上的管理员授权）；需对该 tag 有管理权限 | `deleteOrgTag` | `org_id, tag_id` | 空 |
| 组织管理 | `link_org_tag` | 把已存在的 tag 额外挂到另一个父节点下（DAG 多父，服务端校验防环）；需对目标父节点有管理权限 | `linkOrgTag` | `org_id, parent_tag_id, child_tag_id, rank?` | 空 |
| 组织管理 | `add_org_member` | 把人挂进某个组织节点；需对该节点有管理权限 | `addOrgMember` | `org_id, tag_id, uid, title?, rank?` | 空 |
| 组织管理 | `remove_org_member` | 把人从某个组织节点移除；需对该节点有管理权限 | `removeOrgMember` | `org_id, tag_id, uid` | 空 |
| 组织管理 | `set_org_item_rank` | 调整某条边的排序与职务展示；需对该边所属父节点有管理权限 | `setOrgItemRank` | `org_id, tag_id, child_id, child_type, title?, rank` | 空 |
| 组织管理 | `rename_org` | 重命名组织展示资料；需对组织根有管理权限 | `renameOrg` | `org_id, name, avatar?` | 空 |
| 组织管理 | `grant_org_admin` | 授予某人管理某节点为根子树的权限；需调用方自己已对该节点有管理权限 | `grantOrgAdmin` | `org_id, scope_tag_id, uid` | 空 |
| 组织管理 | `revoke_org_admin` | 撤销某人对某节点为根子树的管理权限；需调用方自己已对该节点有管理权限 | `revokeOrgAdmin` | `org_id, scope_tag_id, uid` | 空 |
| 组织管理 | `list_org_admins` | 列出某节点为根子树的直属管理员（不含挂在祖先节点、递归覆盖到此的管理员）；需对该节点有管理权限 | `listOrgAdmins` | `org_id, scope_tag_id` | `admin_uids[]` |
| 组织管理 | `delete_org` | 删除整个组织：结构（部门/成员/管理员授权）同步清空，成员通讯录组织行异步清理；需对组织根有管理权限 | `deleteOrg` | `org_id` | 空 |
| 上传 | `upload` | HTTP 文件上传 | `uploadFile` | multipart `file, category` + Bearer token | `url, size` |

### 3.2 对外接口汇总

| 模块 | 接口数 |
|---|:---:|
| 用户 | 6 |
| 登录态 | 3 |
| 通讯录 | 14 |
| 消息 | 13 |
| 群组 | 6 |
| 组织 | 5 |
| 组织管理 | 12 |
| 上传 | 1 |
| **合计** | **60** |

### 3.3 特殊同步语义

- 撤回（`send_message` + `MESSAGE_TYPE_RECALL` + `RecallBody`）：服务端校验后覆盖原消息为撤回占位并写入一条 recall 事件消息；`get_messages` 和 `sync_messages` 返回 `MESSAGE_TYPE_RECALL` 消息，SDK 折叠为原消息占位更新，不把 event 暴露成额外可见消息。
- 消息 / 会话删除：`sync_messages`、`sync_conversations` 返回 `status=0xff` tombstone；持久 SDK 收到后删除本地 `messages` / `conversations` 行并推进游标。Message GC 和 Conversation GC 不推进 `messages_version.gc_safe_seq`，不会返回 `seq_too_old`，历史缺口允许漏同步。
- 免打扰关闭：`sync_mutelist` 返回 `status=0xff` tombstone；持久 SDK 收到后删除本地 `mutelist` 行。Mute GC 清理关闭 tombstone 后推进 `mutelist_version.gc_safe_seq`，过旧游标按重建流程处理。
- `get_contacts`：用于联系人分页读取，不是启动阶段全量快照接口。
- `get_blocklist` / `get_mutelist`：用于管理页分页读取，也用于详情页和当前可见对象的单个 / 多个过滤状态查询，不应在启动阶段全量拉取。
- `sync_contacts` / `sync_blocklist` / `sync_mutelist` / `sync_conversations`：仅持久存储 DataGateway 用于本地副本维护；其中通讯录、屏蔽列表、免打扰按 [`../同步机制方案.md`](../同步机制方案.md) 的过旧游标规则重建，消息和会话按消息式 `seq` 窗口同步且不使用 `seq_too_old`；instant 模式不调用 `sync_*`。

### 3.4 直接调试服务端接口

例如 `get_messages` 的 `type=26`（枚举名 `TYPE_ACTION_GET_MESSAGES`）：

```json
{
  "target": { "uid": "10002" },
  "page": { "limit": 30 },
  "limit": 50
}
```

frame header 中设置 `magic='M'`、`codec` bit0=0（big-endian）、bit1-4=1（version）、bit5-7=0、`reserved=0`、`request_id=1`、`type=26`、`size=body 字节数`，再按 checksum 字节置 0 的规则写入 CRC-8。SDK 解码后的响应示例：

```json
{
  "request_id": "1",
  "ok": true,
  "messages": []
}
```

---

## 附录：新增接口落地清单

新增或修改接口时按以下顺序同步：

1. 明确命名：SDK 使用 camelCase，action 和协议字段使用 snake_case。
2. 更新协议：先改 `internal/protocol/yimsg.proto`，确保独立 Request / Response、`BaseResponse base = 1` 和业务字段号从 10 开始，再运行 `go run ./tools/cmd/protocolgen`。
3. 更新服务端：`internal/ws/connection.go` frame 解码 / action 路由、`internal/service/` 业务和必要的存储读写实现。
4. 更新 SDK：`frontend/src/sdk/client.ts`、`frontend/src/sdk/modules/`、DataGateway 或缓存层。
5. 更新 UIKit：若能力暴露到 UI 或宿主回调，更新 `frontend/src/uikit/` 和相关视图。
6. 更新文档：本文、[`../frontend/sdk/sdk接口说明.md`](../frontend/sdk/sdk接口说明.md)、[`../frontend/uikit/UIKit方案.md`](../frontend/uikit/UIKit方案.md)；若影响同步域，先更新 [`../同步机制方案.md`](../同步机制方案.md)；若影响推送通知，同步 [`../server/推送事件方案.md`](../server/推送事件方案.md)。
7. 更新测试：后端单元 / E2E、前端 unit / sdk / ui 按影响面补齐；实现代码变更后从仓库根目录运行 `./tools/run_all_tests.sh`。

### 分页语义说明

- **展示通道（`get_*`）统一使用不透明 keyset 游标**，请求带 `PageQuery{cursor, direction, limit, around}`，响应带 `PageInfo{start_cursor, end_cursor, has_more_backward, has_more_forward, total}`。游标由服务端按各列表展示序的 keyset 字段编码（base64url），对客户端不透明、原样透传，抗实时重排与中途增删漂移，已彻底取代 `offset` 与 `before_seq/after_seq/around_seq`。
- `direction` 以屏幕方向为准：`PAGE_DIRECTION_FORWARD` 向尾/向下翻（追加，续翻用上一页 `end_cursor`），`PAGE_DIRECTION_BACKWARD` 向头/向上翻（前插，续翻用上一页 `start_cursor`）；空 `cursor` + 方向表示从该端起点开始。展示序：消息 旧→新（取最新页用空游标 + BACKWARD）；会话/屏蔽/免打扰 新→旧；联系人按 `sort_key`（pending 按 `seq` 倒序）；群成员 `role` 倒序、`uid` 升序。
- `page.around` 用于锚点居中定位（搜索结果、引用消息、通知/收藏定位）：messages 传被定位消息的 `msg_id`，contacts 传排序首字母；`around` 与 `cursor` 互斥。`get_messages` 的 `msg_ids` 批量读取与 `page` 互斥，不分页、`page` 信息为空。
- 总数：`PageInfo.total` 给 `get_group_members` 承载成员总数（其余可为 `-1`/未知）；通讯录展示总数仍用专用的 `get_contact_count`。
- **同步通道（`sync_*`）不变**，仍用 `last_seq/limit/rebuild`，响应统一带 `has_more` 和 `cursor_seq`。
- `ConversationTarget` 只表达会话/消息目标；通讯录相关目标使用 `ContactTarget`，单个查询通过 repeated 字段传 1 个元素。
