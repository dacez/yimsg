// Code generated by protocolgen; DO NOT EDIT.
//
// TS notification 是入方向：服务端推 frame 过来。这里生成强类型 handler 接口
// 和 frame 分发；方法不可选，新增通知后 SDK 内部 handler 未实现即编译失败。

import type { Frame } from '../transport/frame';
import {
  BlocklistUpdatedNotification,
  ContactsUpdatedNotification,
  ConversationsClearunreadNotification,
  ConversationsDeleteNotification,
  MessagesDeleteNotification,
  MessagesReceivedNotification,
  MutelistUpdatedNotification,
  OrgUpdatedNotification,
  SessionKickedNotification,
  Type,
} from './yimsg';

export interface NotificationHandler {
  /** messages:received：消息域发生变化 */
  onMessagesReceived(n: MessagesReceivedNotification): void | Promise<void>;
  /** contacts:updated：通讯录发生变化 */
  onContactsUpdated(n: ContactsUpdatedNotification): void | Promise<void>;
  /** session:kicked：当前登录态被踢下线 */
  onSessionKicked(n: SessionKickedNotification): void | Promise<void>;
  /** blocklist:updated：屏蔽列表发生变化 */
  onBlocklistUpdated(n: BlocklistUpdatedNotification): void | Promise<void>;
  /** mutelist:updated：会话免打扰发生变化 */
  onMutelistUpdated(n: MutelistUpdatedNotification): void | Promise<void>;
  /** conversations:clearunread：会话未读被清除，仅清红点不拉取 */
  onConversationsClearunread(n: ConversationsClearunreadNotification): void | Promise<void>;
  /** conversations:delete：会话被删除，命中数据窗口则就地删除不拉取 */
  onConversationsDelete(n: ConversationsDeleteNotification): void | Promise<void>;
  /** messages:delete：消息被删除，命中数据窗口则就地删除不拉取 */
  onMessagesDelete(n: MessagesDeleteNotification): void | Promise<void>;
  /** org:updated：组织架构发生变化，客户端按 org_id 增量同步追平 */
  onOrgUpdated(n: OrgUpdatedNotification): void | Promise<void>;
}

export type NotificationDispatchResult =
  | { ok: true; handled: true; typeId: number }
  | { ok: true; handled: false }
  | { ok: false; handled: false; typeId: number; error: Error };

export async function dispatchNotificationFrame(
  handler: NotificationHandler,
  frame: Frame,
): Promise<NotificationDispatchResult> {
  if (frame.requestId !== '0') return { ok: true, handled: false };
  switch (frame.typeId) {
    case Type.TYPE_NOTIFY_MESSAGES_RECEIVED: {
      try {
        const n = MessagesReceivedNotification.decode(frame.body);
        await handler.onMessagesReceived(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_CONTACTS_UPDATED: {
      try {
        const n = ContactsUpdatedNotification.decode(frame.body);
        await handler.onContactsUpdated(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_SESSION_KICKED: {
      try {
        const n = SessionKickedNotification.decode(frame.body);
        await handler.onSessionKicked(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_BLOCKLIST_UPDATED: {
      try {
        const n = BlocklistUpdatedNotification.decode(frame.body);
        await handler.onBlocklistUpdated(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_MUTELIST_UPDATED: {
      try {
        const n = MutelistUpdatedNotification.decode(frame.body);
        await handler.onMutelistUpdated(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_CONVERSATIONS_CLEARUNREAD: {
      try {
        const n = ConversationsClearunreadNotification.decode(frame.body);
        await handler.onConversationsClearunread(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_CONVERSATIONS_DELETE: {
      try {
        const n = ConversationsDeleteNotification.decode(frame.body);
        await handler.onConversationsDelete(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_MESSAGES_DELETE: {
      try {
        const n = MessagesDeleteNotification.decode(frame.body);
        await handler.onMessagesDelete(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    case Type.TYPE_NOTIFY_ORG_UPDATED: {
      try {
        const n = OrgUpdatedNotification.decode(frame.body);
        await handler.onOrgUpdated(n);
        return { ok: true, handled: true, typeId: frame.typeId };
      } catch (err) {
        return { ok: false, handled: false, typeId: frame.typeId, error: err instanceof Error ? err : new Error(String(err)) };
      }
    }
    default:
      return { ok: true, handled: false };
  }
}

// NoopNotificationHandler 提供空实现，便于测试或可选处理；SDK 核心不得依赖它掩盖漏处理。
export class NoopNotificationHandler implements NotificationHandler {
  onMessagesReceived(): void {}
  onContactsUpdated(): void {}
  onSessionKicked(): void {}
  onBlocklistUpdated(): void {}
  onMutelistUpdated(): void {}
  onConversationsClearunread(): void {}
  onConversationsDelete(): void {}
  onMessagesDelete(): void {}
  onOrgUpdated(): void {}
}
