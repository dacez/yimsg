# BoundedList 缺陷列表

> 主要对照：`packages/uikit/src/app/bounded-list/`、`packages/uikit/src/app/views/{list-pill,local-page-source}.ts`、`apps/web/tests/component/bounded-list.spec.ts` 与 `apps/web/tests/performance/bounded-list.performance.spec.ts`。
> 最后复核：2026-08-23。
> 触发更新：任一缺陷新发现、复现、修复、重新定性，或对应 Playwright 断言、数据规模、阈值、浏览器版本变化时同步更新。
> 入口关系：专题入口见 [`README.md`](README.md)，整体模型见 [`导读.md`](导读.md)，接口契约见 [`组件设计.md`](组件设计.md)，单元与浏览器测试口径见 [`测试方案.md`](测试方案.md)，仓库测试矩阵见 [`../../../../docs/development/测试方案.md` §6](../../../../docs/development/测试方案.md#6-boundedlist-playwright-与性能专项)。

本文是缺陷登记入口与状态维护规则的当前事实源。2026-08-13 及以前已关闭缺陷的复现条件、根因、修复摘要和关闭证据归档在 [`../../../../docs/archive/BoundedList缺陷列表.md`](../../../../docs/archive/BoundedList缺陷列表.md)，本文不重复维护那份历史内容；那份归档是冻结快照、不再追加，此后新登记的缺陷（含已关闭的）一律留在本文 §1，最新一轮在 §1.1。

## 1. 当前状态

当前没有 `OPEN` 缺陷。累计 53 条缺陷已全部关闭：18 条 `BL-BUG-NNN` 产品缺陷（真实 Chromium 与代码复审发现）、32 条 `BL-UNIT-BUG-NNN` 产品缺陷（单元评审发现）、3 条 `BL-TEST-BUG-NNN` 测试基础设施缺陷。全部正确期望断言仍以普通回归测试保留，没有 `test.fail`、`skip` 或 `fixme`。

> 编号按发现层级区分：`BL-BUG-NNN` 是真实 Chromium 产品缺陷，`BL-UNIT-BUG-NNN` 是单元评审产品缺陷，`BL-TEST-BUG-NNN` 是测试基础设施缺陷。

### 1.1 本轮（2026-08-23 架构复审）新登记并关闭

两条都由「组件边界」复审发现：功能本身没写错，但**放错了层**，于是产生了没人察觉的死配置和跨对象隐式契约。随组件边界收敛（提示条、防抖、本地切片源移到宿主侧）一并修复。

| ID | 严重度 | 状态 | 缺陷与修复摘要 | 回归用例 |
|---|---|---|---|---|
| `BL-UNIT-BUG-031` | P2 | CLOSED | 十二个生产列表全部配了 `text.retry`，但其中七个传了 `pillHost: false` —— 提示条句柄退化为空操作，那七处重试文案永远不显示，首屏失败后用户没有任何重试入口，只能切 tab / 重开弹窗。根因是组件替宿主决定 UI：「配了却不生效」不会报错也不会被类型系统拦住。提示条整体移到宿主侧 `views/list-pill.ts`，组件只报 `stale` / `failed` 并提供 `catchUp()`，不需要提示条的七个列表直接不接，死配置消失。 | `bounded-list` F3、`views/list-pill` A4 |
| `BL-UNIT-BUG-032` | P2 | CLOSED | `localPageSource` 自带 `order` 选项，且必须与所属 BoundedList 的 `order` 一致，不一致会**静默取错端**（`order:'asc'` 的列表首屏显示最旧一页）。根因是首页请求硬编码 `backward: false`，没有携带新鲜端方向，数据源只能自己再配一份展示序。`FetchPageRequest` 增加 `freshEdge`，数据源按请求取端，`order` 选项删除，跨对象隐式契约从根上消失。 | `views/local-page-source` D1–D4 |

### 1.2 上一轮（2026-08-17 源码复审）

四条都由源码复审发现，回归落在单元层；`../../../../docs/archive/BoundedList缺陷列表.md` 是 2026-08-13 的冻结快照，不再追加，所以此后各轮的复现与修复摘要都留在本文。

| ID | 严重度 | 状态 | 缺陷与修复摘要 | 回归用例 |
|---|---|---|---|---|
| `BL-UNIT-BUG-027` | P2 | CLOSED | 行复用判据按 `reuseUnchangedRows` 分成两套：内部数据更新那条**不检查条目是否相等**，于是「条目数据变了但 `renderItem` 产出的 DOM 恰好一致」时保留旧节点，宿主挂在旧节点上的监听仍闭包着旧数据。统一为「条目数据一变就换节点」，两种模式同一判据。 | `renderer` A7b/A7c |
| `BL-UNIT-BUG-028` | P2 | CLOSED | `removeLocal` 对只存在于 `pinnedItems()` 里的身份无效：`overlay.apply` 只作用于权威窗口，pinned 由 `visibleItems()` 单独拼接，于是该方法返回 `true` 却删不掉，下一帧又被钉回来。`LocalOverlay` 增加 `isDropped(id)`，pinned 段一并过滤。 | `bounded-list` H1b/H1c |
| `BL-UNIT-BUG-029` | P2 | CLOSED | 后台追平失败与首屏失败共用 `onError` 的 `'reset'` 阶段值，宿主无法区分「用户面前没内容」和「旧内容还在、只是没追上」，消息列表因此在网络抖动时弹出「加载消息失败」。新增 `'catchup'` 阶段值，保留窗口的首页请求失败时上报它。 | `bounded-list` J1b |
| `BL-UNIT-BUG-030` | P3 | CLOSED | `SelectionStore.replaceSingle` 在选中集已经正是该项时仍 `notify()`，单选列表里重复点同一行会触发整表重跑 `renderItem`。与同文件的 `delete` / `clear` 对齐，无变化即不通知。 | `selection` C1b/C1c |

### 1.3 更早

`BL-BUG-018`（发送消息后会话列表提示条一闪而过），随 2026-08-13 的组件重构一并修复，回归见 `bounded-list` E7 与 `BL-PW-015`。

独立复测命令：

```bash
# 功能
./tools/run_component_tests.sh

# 性能 / 容量
./tools/run_performance_tests.sh
```

## 2. 状态与维护规则

| 状态 | 含义 |
|---|---|
| `OPEN` | 已在真实 Chromium 稳定复现，尚无通过的正确期望回归 |
| `FIXED_PENDING_VERIFY` | 已有修复，但目标浏览器、性能门禁或全量回归尚未完成 |
| `CLOSED` | 正确期望的普通回归已通过，且没有 `test.fail` / `skip` / `fixme` 掩盖 |
| `DESIGN_EXPECTED` | 经契约确认属于设计行为，并保留重新定性的依据 |

新增缺陷时必须同时记录稳定编号、严重度、环境、前置条件、步骤、期望、实际、影响、定位和可执行回归。关闭缺陷时必须：

1. 保留原正确期望断言，移除临时 `test.fail`。
2. 同步本文总览、测试矩阵和设计契约；复现、修复摘要与关闭证据写进 §1.1（归档文件已冻结，不再追加）。
3. 运行 33 项功能专项、6 项性能专项以及仓库全量测试；全量脚本通过 `run_component_tests.sh` 包含功能专项，但性能专项仍由 `run_performance_tests.sh` 独立执行。

产品缺陷继续使用 `BL-BUG-NNN`；测试夹具、调度和断言自身的缺陷使用 `BL-TEST-BUG-NNN`，不得把测试基础设施问题记成产品缺陷，也不得用降低 worker 或增加 retry 作为关闭依据。
