# Yimsg Official Web App

官方完整 Web App 只通过 `@yimsg/uikit` workspace 使用 UI 和 SDK 能力，生产产物仍输出到仓库根目录 `web/app/`。

```bash
npm run build -w @yimsg/web
npm run test -w @yimsg/web
```

本应用采用 `AGPL-3.0-only`。
